/* 
// modelViewer.js
export function initModelViewers() {
    // This function gets called once when the page loads
    // It will find any model containers in the page and initialize them
    setupEventListeners();
  }
  
  export function loadSTLModel(container, modelConfig) {
    if (!window.THREE) {
      console.error('THREE.js not loaded. Make sure to include it in your HTML.');
      return;
    }
    
    // ... rest of the file ...
  }
  
  function createFullscreenViewer(modelConfig) {
    // ... rest of the file ...
  }
  
  function applyFullscreenStyles(fullscreenView, closeButton, modelContainer) {
    // ... rest of the file ...
  }
  
  function setupEventListeners() {
    // ... rest of the file ...
  }
*/
